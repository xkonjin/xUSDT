import React from 'react';

// Mock lucide-react icons for testing
export const X = ({ className }: { className?: string }) => (
  <svg data-testid="x-icon" className={className}><path /></svg>
);

export const Loader2 = ({ className }: { className?: string }) => (
  <svg data-testid="loader-icon" className={className}><path /></svg>
);
