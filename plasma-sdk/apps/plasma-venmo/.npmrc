# Disable npm workspaces for Vercel deployment
# Vercel doesn't support workspace:* protocol
# We'll use local package.json for internal deps
workspaces=false

# Use local registry first
registry=https://registry.npmjs.org
